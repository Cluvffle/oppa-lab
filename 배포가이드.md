# 오빠 매력연구소 웹사이트 배포 가이드

## 전체 순서
Google Sheets 설정 → GitHub 업로드 → Vercel 배포 → 환경변수 설정

---

## STEP 1. Google Sheets + Apps Script 설정

1. **구글 드라이브** (drive.google.com) → 새 스프레드시트 생성
2. 시트 탭 이름을 **"신청서"** 로 변경
3. 상단 메뉴 → **확장 프로그램 → Apps Script**
4. 기존 코드 전부 지우고 `google-apps-script.js` 파일 내용 붙여넣기
5. **저장** (Ctrl+S)
6. 상단 **"배포"** 버튼 → **"새 배포"**
7. 설정:
   - 유형: **웹 앱**
   - 실행 계정: **나 (본인)**
   - 액세스 권한: **모든 사용자**
8. **"배포"** 클릭 → 나타나는 URL 복사해두기
   - 예) `https://script.google.com/macros/s/ABC123.../exec`

---

## STEP 2. GitHub 업로드

> GitHub 계정이 없으면 github.com 에서 무료 가입

1. **github.com** → New repository → 이름: `oppa-lab`
2. **Public** 선택 → Create repository
3. 내 컴퓨터에서 터미널(맥: 터미널, 윈도우: PowerShell) 열기
4. website 폴더로 이동:
   ```
   cd "Documents/Claude/Projects/oppa-lab/website"
   ```
5. Git 초기화 & 업로드:
   ```
   git init
   git add .
   git commit -m "first commit"
   git branch -M main
   git remote add origin https://github.com/내-아이디/oppa-lab.git
   git push -u origin main
   ```

---

## STEP 3. Vercel 무료 배포

1. **vercel.com** 접속 → 구글 계정으로 로그인
2. **"Add New Project"** 클릭
3. GitHub 연결 → `oppa-lab` 레포지토리 선택
4. **"Deploy"** 클릭 (자동으로 빌드됨, 약 1분)
5. 배포 완료 → 주소 확인
   - 기본 주소: `oppa-lab.vercel.app` (무료!)

---

## STEP 4. 환경변수 설정 (구글시트 연동)

1. Vercel 대시보드 → 프로젝트 선택
2. **Settings → Environment Variables**
3. 추가:
   - Name: `VITE_APPS_SCRIPT_URL`
   - Value: STEP 1에서 복사한 Apps Script URL
4. **Save** → 상단 **Deployments** → **Redeploy** 클릭

---

## 완료!

- 웹사이트: `oppa-lab.vercel.app`
- 신청서: 구글 스프레드시트에 자동 저장
- 새 신청 시: 본인 구글 이메일로 알림 발송

---

## 도메인 관련 안내

| 옵션 | 비용 | 주소 예시 |
|------|------|-----------|
| Vercel 기본 (무료) | 무료 | oppa-lab.vercel.app |
| 커스텀 도메인 | 연 1~2만원 | oppalab.kr / oppalab.co.kr |

커스텀 도메인은 Vercel Settings → Domains 에서 연결 가능해요.
한국 도메인은 가비아(gabia.com)에서 저렴하게 구매할 수 있어요.

---

## 자주 있는 문제

**Q. 구글시트에 데이터가 안 쌓여요**
→ Apps Script 재배포 필요. 배포 → "배포 관리" → 수정 아이콘 → 버전 새 버전으로 변경 후 재배포

**Q. 빌드 에러가 나요**
→ Vercel 배포 로그 확인. 대부분 node_modules 관련 문제로 자동 해결됨

**Q. 주소를 바꾸고 싶어요**
→ Vercel Settings → General → Project Name 변경
